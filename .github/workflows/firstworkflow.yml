name: Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
       -name: greeting
       -run: echo "hello good morning"
       -name: hi
       -run: "gooafternoon"
      
